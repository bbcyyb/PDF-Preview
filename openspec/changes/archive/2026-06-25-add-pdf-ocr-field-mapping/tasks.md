## 1. 项目搭建

- [x] 1.1 在项目根目录创建 Vite + React + TypeScript 应用结构。
- [x] 1.2 添加用于开发、类型检查、构建和预览应用的 package 脚本。
- [x] 1.3 添加 PDF 渲染依赖，并在单一应用入口中配置 PDF.js worker。
- [x] 1.4 为左侧表单面板和右侧 PDF 预览面板创建基础应用样式。

## 2. OCR 数据模型

- [x] 2.1 为 OCR 文档、字段、页码、边界框和来源页面尺寸定义 TypeScript 类型。
- [x] 2.2 为简单 MVP 文档定义确定性的 fixture 字段来源，包括标签、值、PDF 位置和文本尺寸。
- [x] 2.3 编写一个简单的中文 Markdown 测试文档，例如 `fixtures/source/sample-document.zh.md`，供用户创建 Word 文档并导出为 PDF。
- [x] 2.4 确保中文测试文档包含一组少量且稳定的字段，适合表单展示和 PDF 高亮，例如文档编号、申请人姓名、日期、金额和审批状态。
- [x] 2.5 添加 fixture 生成脚本，根据确定性字段来源创建 `public/fixtures/sample-document.pdf`。
- [x] 2.6 使用同一个 fixture 生成脚本或配套脚本，根据确定性字段来源创建 `public/fixtures/sample-document.ocr.json`，用于模拟已经完成的 OCR 输出。
- [x] 2.7 确保生成的 OCR fixture 坐标使用左上角原点、像素单位、从 1 开始计数的页码、`bbox`、`sourcePageWidth` 和 `sourcePageHeight`。
- [x] 2.8 添加随项目提供的示例 OCR JSON 数据，包含有效的字段标签、值、页码、bbox 坐标和来源页面尺寸。
- [x] 2.9 记录示例 PDF fixture 与 OCR JSON fixture 的关系，使后续测试能够验证坐标对齐。
- [x] 2.10 添加校验或保护逻辑，用于检测缺少足够坐标数据、无法安全生成高亮的字段。
- [x] 2.11 实现纯坐标缩放工具函数，将 OCR 来源坐标映射到 PDF 页面渲染坐标。

## 3. PDF 预览

- [x] 3.1 实现随项目提供的 fixture PDF 文件加载。
- [x] 3.2 在右侧预览面板中按文档顺序渲染 PDF 页面。
- [x] 3.3 跟踪每个已渲染页面的实际宽度和高度，用于 overlay 坐标转换。
- [x] 3.4 当 PDF 加载或渲染失败时显示可见的错误状态。

## 4. OCR 表单界面

- [x] 4.1 在左侧面板中以表单形式渲染 OCR 字段，显示标签和提取值。
- [x] 4.2 未加载 OCR 字段时显示清晰的空状态。
- [x] 4.3 维护一个由表单和 PDF 预览共享的单一选中字段状态。
- [x] 4.4 为当前选中的表单字段应用可见的选中样式。

## 5. 字段到 PDF 的高亮映射

- [x] 5.1 为每个已渲染的 PDF 页面添加对齐的 overlay 层。
- [x] 5.2 当有效坐标数据可用时，为选中字段绘制可见高亮。
- [x] 5.3 当选择不同字段时，清除之前的高亮。
- [x] 5.4 将 PDF 预览滚动或导航到选中字段的来源页面。
- [x] 5.5 当渲染页面尺寸变化时，重新计算高亮位置。

## 6. 自动化测试

- [x] 6.1 添加适用于 React + TypeScript 项目的单元测试运行器，例如 Vitest。
- [x] 6.2 为坐标缩放添加单元测试，覆盖同尺寸、放大、缩小和小数渲染页面尺寸。
- [x] 6.3 为 OCR 字段校验添加单元测试，覆盖缺少 bbox、缺少来源尺寸、无效页码和有效可映射字段等情况。
- [x] 6.4 为 OCR 表单渲染、空状态渲染、选中字段样式和选择状态变化添加组件测试。
- [x] 6.5 添加组件测试或集成测试，验证选择字段后会根据 fixture OCR 数据创建预期的高亮 overlay。
- [x] 6.6 添加基于 fixture 的测试，加载生成的 OCR JSON fixture，并断言每个可映射字段都引用现有 PDF 页面且具有有效 bbox。
- [x] 6.7 添加测试，验证 fixture 生成输出保持稳定；当确定性字段来源变化时，测试必须失败并提供有意更新路径。

## 7. 真实 fixture 验证

- [x] 7.1 在开发环境中使用生成的 PDF 和 OCR fixture 作为默认演示数据集。
- [ ] 7.2 手动验证每个 fixture 字段是否高亮到 PDF 预览中的预期视觉位置。
- [x] 7.3 记录 fixture 验证说明，包括已知坐标容差或 OCR 限制。
- [x] 7.4 确保真实 PDF fixture 可用但缺少匹配 OCR fixture 数据时，应用显示有用状态。

## 8. 响应式行为与验证

- [x] 8.1 确保桌面布局在左侧显示 OCR 表单，在右侧显示 PDF 预览。
- [x] 8.2 确保窄视口仍然可用，且不隐藏表单或预览。
- [x] 8.3 标记实现完成前，运行所有单元测试、组件测试和基于 fixture 的测试。
- [x] 8.4 标记实现完成前，运行类型检查和生产构建。
- [ ] 8.5 手动验证选择字段后，PDF 高亮和页面定位是否正确更新。
